<.header>
  Listing Events
</.header>
<ul id="events" phx-update="stream">
  <li :for={{dom_id, event} <- @streams.events} id={dom_id}>
    <%= event.serviceName %>, <%= event.environmnet %>, <%= event.timestamp %>
  </li>
</ul>
